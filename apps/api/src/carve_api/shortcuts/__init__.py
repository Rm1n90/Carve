# Armin Mehri -- mehri.armin@gmail.com
"""Per-user keyboard shortcut overrides.

See ``router.py`` for the HTTP API. The storage shape is a sparse
JSONB map on ``users.shortcut_overrides`` (migration 0030).
"""
